import shelver from "../instructions/shelver";

const x = shelver[0].steps

function Message(){

    return<>
    <button type="button" className="btn btn-info">Base class</button>
    Inside message component
    </>
}

export default Message;